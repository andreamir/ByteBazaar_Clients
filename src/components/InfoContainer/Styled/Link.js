import { styled } from 'styled-components'

const Link = styled.a`
  color: #000;
  text-decoration: underline;
  font-size: 16px;
  font-weight: 600;
  line-height: 20px;
  cursor: pointer;
`

export default Link